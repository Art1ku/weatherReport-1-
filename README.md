# WaveWeather 🌤️

Простое и интуитивное веб-приложение для проверки прогноза погоды в реальном времени для любого города мира.

## 🚀 Быстрый старт

### Онлайн
Откройте приложение напрямую: [WaveWeather](https://Art1ku.github.io/weatherReport-1-)

### Локально
```bash
# 1. Клонировать репозиторий
git clone https://github.com/Art1ku/weatherReport-1-.git
cd weatherReport

# 2. Открыть в браузере
# Вариант 1: Двойной клик на index.htm
# Вариант 2: Открыть через Live Server в VS Code
# Вариант 3: Использовать локальный сервер
python -m http.server 8000  # Python 3
# или
python -m SimpleHTTPServer 8000  # Python 2
```

## ✨ Функции

- 🔍 **Поиск по городам** — введите название города на английском языке
- 🌡️ **Текущая температура** — крупное отображение с эмодзи
- 💨 **Детальная информация** — скорость ветра, влажность, часовой пояс
- 📅 **Прогноз на неделю** — предстоящие условия погоды
- 🎨 **Динамические фоны** — фоновое изображение зависит от погоды
- ⚡ **Реальное время** — данные обновляются мгновенно
- 📱 **Адаптивный дизайн** — работает на любых устройствах

## 🛠️ Технологический стек

| Технология | Назначение |
|-----------|-----------|
| **HTML5** | Структура приложения |
| **CSS3** | Стилизация, адаптивный дизайн, Flexbox |
| **JavaScript (ES6+)** | Логика, работа с API, обработка событий |
| **Open-Meteo API** | Данные о погоде |
| **Geocoding API** | Преобразование города в координаты |

## 📋 Использованные API

### 1. Open-Meteo Geocoding API
Преобразует название города в географические координаты

```javascript
https://geocoding-api.open-meteo.com/v1/search?name={city}&count=1&language=ru&format=json
```

**Пример:**
```bash
curl "https://geocoding-api.open-meteo.com/v1/search?name=Bishkek&count=1&language=ru&format=json"
```

### 2. Open-Meteo Weather API
Получает текущие данные о погоде и прогноз

```javascript
https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&current=temperature_2m,relative_humidity_2m,wind_speed_10m,weather_code&daily=weather_code,temperature_2m_max,temperature_2m_min&timezone=auto
```

**Преимущества:**
- ✅ Бесплатный API без регистрации
- ✅ Высокая точность данных
- ✅ Поддержка русского языка
- ✅ Без ограничений на количество запросов
- ✅ CORS включен

## 📁 Структура проекта

```
weatherReport/
├── index.htm          # Главная HTML страница
├── style.css          # Стили приложения
├── script.js          # Основная логика JavaScript
├── README.md          # Этот файл
├── public/
│   └── logo.svg       # Логотип приложения
└── .git/              # Репозиторий Git
```

## 🎨 Цветовая палитра

| Цвет | Код | Использование |
|------|-----|---------------|
| Черный | `#000000` | Фон |
| Белый | `#FFFFFF` | Текст и элементы |
| Полупрозрачный черный | `rgba(0, 0, 0, 0.35)` | Оверлей |

## 🔧 Основной код

### Функция загрузки погоды

```javascript
async function loadByCity(city) {
  try {
    toggleLoading(true);
    hideError();

    // Шаг 1: Получение координат города
    const geoUrl = `https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1&language=ru&format=json`;
    const geoRes = await fetch(geoUrl);
    const geoData = await geoRes.json();
    const place = geoData?.results?.[0];

    if (!place) throw new Error("Город не найден");

    // Шаг 2: Получение данных о погоде
    const wUrl = `https://api.open-meteo.com/v1/forecast?latitude=${place.latitude}&longitude=${place.longitude}&current=temperature_2m,relative_humidity_2m,wind_speed_10m,weather_code&daily=weather_code,temperature_2m_max,temperature_2m_min&timezone=auto`;
    
    const wRes = await fetch(wUrl);
    const wData = await wRes.json();

    // Шаг 3: Отображение результатов
    renderCurrent(place, wData.current, wData.timezone);
    renderForecast(wData.daily);
  } catch (err) {
    showError(err.message || "Что-то пошло не так");
  } finally {
    toggleLoading(false);
  }
}
```

### Объект кодирования погоды

```javascript
const WEATHER = {
  0: { text: "Ясно", emoji: "☀️", background: "..." },
  1: { text: "Преимущественно ясно", emoji: "🌤️", background: "..." },
  2: { text: "Переменная облачность", emoji: "⛅", background: "..." },
  3: { text: "Пасмурно", emoji: "☁️", background: "..." },
  45: { text: "Туман", emoji: "🌫️", background: "..." },
  63: { text: "Дождь", emoji: "🌧️", background: "..." },
  71: { text: "Снег", emoji: "🌨️", background: "..." },
  95: { text: "Гроза", emoji: "⛈️", background: "..." },
  // ... и еще 10+ типов
};
```

## 🎯 Как использовать

1. **Откройте приложение** в браузере
2. **Введите название города** в поле поиска (на английском)
3. **Нажмите Enter или кнопку поиска**
4. **Просмотрите информацию:**
   - Текущая температура и тип погоды
   - Ветер, влажность, часовой пояс
   - Прогноз на 7 дней

## 📱 Примеры городов для тестирования

```
Bishkek
Moscow
New York
London
Tokyo
Sydney
Berlin
Paris
Dubai
Bangkok
```

## ⚠️ Важные замечания

- Приложение работает полностью на **фронтенде**, без собственного бэкенда
- Все запросы идут напрямую к Open-Meteo API
- Введение названия города должно быть на **английском языке**
- Приложение требует интернет-соединение для работы API

## 🚀 Развертывание

### На GitHub Pages

```bash
# 1. Убедитесь, что репозиторий публичный
# 2. Перейдите в Settings → Pages
# 3. Выберите ветку "main" как источник
# 4. Сохраните изменения
# 5. Приложение будет доступно по адресу:
# https://Art1ku.github.io/weatherReport-1-
```

### На других хостингах

Приложение можно развернуть на:
- **Netlify** — перетащите папку проекта
- **Vercel** — подключите GitHub репозиторий
- **Cloudflare Pages** — выберите репозиторий и deploy
- **Firebase Hosting** — используйте `firebase deploy`

## 🐛 Возможные ошибки

| Ошибка | Решение |
|--------|---------|
| "Город не найден" | Проверьте название на английском языке |
| "Не удалось получить погоду" | Проверьте интернет-соединение |
| "Ошибка геокодинга" | API временно недоступен, попробуйте позже |
| Пустые данные | Перезагрузите страницу |

## 📈 Возможные улучшения

- [ ] Добавить историю поиска (localStorage)
- [ ] Карта с геолокацией (Leaflet.js)
- [ ] Уведомления об экстремальной погоде
- [ ] Темная и светлая тема
- [ ] Поддержка нескольких языков
- [ ] Прогноз на месяц
- [ ] Индекс качества воздуха (AQI)
- [ ] Синхронизация с календарем

## 📚 Ресурсы и документация

- [Open-Meteo API Documentation](https://open-meteo.com/en/docs)
- [Open-Meteo Geocoding API](https://open-meteo.com/en/docs/geocoding-api)
- [Weather Code Reference](https://open-meteo.com/en/docs#weather_code)
- [MDN Web Docs - Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [CSS Flexbox Guide](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)

## 👨‍💻 Автор

**Art1ku**
- GitHub: [@Art1ku](https://github.com/Art1ku)
- Repository: [weatherReport-1-](https://github.com/Art1ku/weatherReport-1-)

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Вы можете свободно использовать, изменять и распространять его.

## 🤝 Вклад

Если вы нашли ошибку или хотите предложить улучшение:

1. **Откройте Issue** с описанием проблемы
2. **Создайте Pull Request** со своими изменениями
3. **Обсудите** идеи и предложения

## 📞 Поддержка

Если у вас есть вопросы или нужна помощь:

- Создайте **Issue** в репозитории
- Отправьте **Email** [ваш email]
- Свяжитесь через **социальные сети**

---

**Спасибо за использование WaveWeather! 🌦️**

Сделано с ❤️ на HTML5, CSS3 и JavaScript
